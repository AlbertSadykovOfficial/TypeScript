{
  "compilerOptions": {
  	// Какие API выполнения должны поддерживаться (dom, "esnext", "webworker")
    "lib": ["es2015"],
    // Выходной стандарт
    "target": "es5",
    // В какую модульную систему (commonjs, systemjs, es2015) нужно компилировать 
    "module": "commonjs",
    // Выходной каталг для компиляции
    "outDir": "dist",
    // максимально строгая проверка кода - true
    "strict": true
  },
  // В каких каталогах искать файлы
  "include": [
  	"src"
  ]
}